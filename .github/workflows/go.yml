# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go

on:
  push:
    branches: [ "test" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
       
   # - name: Set up Go
    #  uses: actions/setup-go@v4
     # with:
      #  go-version: '1.21.5'
    - name: Install LXD
    
      run: 
        #snap help refresh
        #sudo snap install lxd
        sudo systemctl start snap.lxd.daemon.unix.socket
        sudo systemctl status snap.lxd.daemon.unix.socket
        #sudo lxc launch ubuntu:22.04 ubuntu-container
      working-directory: test
   
    - name: test1
      run: #echo "hey" | lxc config trust add >>hey2
         # ls -ll /var/snap/lxd/common/
        sudo cat config.yaml | sudo lxd init --preseed
        echo "hey" | lxc config trust add >>hey2
      working-directory: test

    - name: Build
      run: go build 

    - name: Test
      run: go test 
  
